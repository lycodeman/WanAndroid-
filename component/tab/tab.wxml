<!--component/tab/tab.wxml-->
<view class="row">
  <text wx:for="{{tabs}}" wx:for-item="tab" wx:key="name" bind:tap="onTab" data-tab="{{tab}}" data-index="{{index}}" style="{{index == curIndex ? tab.selectStyle : tab.unSelectStyle}}">{{tab.name}}</text>
</view>